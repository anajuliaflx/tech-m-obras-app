import React from 'react';
import TableStructure from './TableStructure';

export default function TableConstruction({ style }: { style: string }) {
  return (
    <TableStructure style={style} />
  );
}